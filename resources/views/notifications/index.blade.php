@extends('layouts.app')
@section('title', 'Notifications | FPNE Micro-Internship')
@section('content')<main class="container py-5"><div class="d-flex flex-wrap justify-content-between align-items-end mb-4"><div><p class="text-uppercase small text-secondary mb-1">Your inbox</p><h1 class="h2 mb-0">Notifications</h1></div><span class="badge rounded-pill text-bg-light px-3 py-2">{{ $notifications->count() }} recent</span></div>@forelse($notifications as $notification)<article class="card border-0 shadow-sm mb-3 {{ $notification->read_at ? '' : 'border-start border-4 border-warning' }}"><div class="card-body d-flex flex-wrap justify-content-between gap-3"><div><h2 class="h5">{{ $notification->title }}</h2><p class="mb-1">{{ $notification->message }}</p><time class="small text-secondary">{{ $notification->created_at->toDateTimeString() }}</time></div>@if(!$notification->read_at)<form method="POST" action="{{ route('notifications.read', $notification) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-primary" type="submit">Mark as read</button></form>@else<span class="badge text-bg-success align-self-start">Read</span>@endif</div></article>@empty<div class="alert alert-light border">No notifications yet.</div>@endforelse</main>@endsection