@extends('layouts.app')
@section('title', 'Interviews | FPNE Micro-Internship')
@section('content')
<main class="container py-5"><div class="mb-4"><p class="text-uppercase small text-secondary mb-1">Schedule</p><h1 class="h2 mb-1">Interviews</h1><p class="text-secondary mb-0">Your upcoming conversations and meeting links.</p></div><div class="row g-3">@forelse($interviews as $interview)<div class="col-md-6"><article class="card border-0 shadow-sm h-100"><div class="card-body p-4"><div class="d-flex justify-content-between gap-3"><div><h2 class="h5 mb-1">{{ $interview->application->internship->title }}</h2><p class="text-secondary mb-3">{{ auth()->user()->role === 'student' ? 'With '.$interview->application->internship->supervisor->name : 'With '.$interview->application->student->name }}</p></div><span class="badge text-bg-light align-self-start">{{ ucfirst($interview->status) }}</span></div><p class="mb-1"><strong>{{ $interview->scheduled_at->format('D, M j, Y · g:i A') }}</strong></p><p class="text-secondary mb-3">{{ $interview->duration_minutes }} minutes</p>@if($interview->meeting_url)<a class="btn btn-primary btn-sm" href="{{ $interview->meeting_url }}" target="_blank" rel="noreferrer">Join meeting</a>@endif @if($interview->status === 'scheduled')<form class="d-inline" method="POST" action="{{ route('interviews.cancel', $interview) }}">@csrf @method('PATCH')<button class="btn btn-outline-danger btn-sm">Cancel</button></form>@endif</div></article></div>@empty<div class="col-12"><div class="alert alert-light border">No interviews scheduled yet.</div></div>@endforelse</div></main>
@endsection
