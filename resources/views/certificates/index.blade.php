@extends('layouts.app')
@section('title', 'Certificates | FPNE Micro-Internship')
@section('content')
<main class="container py-5"><div class="mb-4"><p class="text-uppercase small text-secondary mb-1">Your achievements</p><h1 class="h2 mb-1">Certificates</h1><p class="text-secondary mb-0">Download a verified record of each completed project.</p></div><div class="row g-3">@forelse($certificates as $certificate)<div class="col-md-6"><article class="card border-0 shadow-sm h-100"><div class="card-body p-4"><span class="badge text-bg-success mb-3">Verified completion</span><h2 class="h5">{{ $certificate->internship->title }}</h2><p class="text-secondary">Issued {{ $certificate->issued_at->toFormattedDateString() }}</p><p class="small text-secondary mb-3">Certificate {{ $certificate->certificate_number }}</p><a class="btn btn-primary btn-sm" href="{{ route('certificates.download', $certificate) }}">Download certificate</a><a class="btn btn-outline-primary btn-sm ms-2" href="{{ route('certificates.verify', $certificate->verification_hash) }}">Verify online</a></div></article></div>@empty<div class="col-12"><div class="alert alert-light border">Certificates will appear here after all project milestones are verified.</div></div>@endforelse</div></main>
@endsection
